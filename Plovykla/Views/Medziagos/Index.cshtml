@model IEnumerable<Plovykla.Models.Medziagos>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutDarbuotojas.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.medziaga)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.medziaga)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.medziagosId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.medziagosId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.medziagosId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
